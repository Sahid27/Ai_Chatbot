export async function POST(req) {
  try {
    const { message } = await req.json();

    const response = await fetch(
      "https://api.groq.com/openai/v1/chat/completions",
      {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          Authorization: `Bearer ${process.env.GROQ_API_KEY}`,
        },
        body: JSON.stringify({
          model: "llama-3.1-8b-instant",
          messages: [
            {
              role: "system",
              content: `
তুমি একজন বন্ধুসুলভ, বুদ্ধিমান এবং অনুভূতিশীল AI chatbot।
তোমার কাজ হলো ইউজারকে ঠিক মানুষের মতো বুঝে উত্তর দেওয়া।

তোমাকে যেভাবে কথা বলতে হবে:
- ইউজার যে ভাষায় লিখবে, ঠিক সেই ভাষায় উত্তর দেবে
- বাংলা লিখলে → বাংলায়
- ইংরেজিতে লিখলে → ইংরেজিতে
- ইংরেজি অক্ষরে বাংলা (Banglish) লিখলে → Banglish-এ
- ইউজার মজা করলে → হালকা ফানি ও বন্ধুসুলভ হও
- ইউজার সিরিয়াস হলে → সিরিয়াস, পরিষ্কার ও সাহায্যকারী হও
- ইউজার মন খারাপ বা ইমোশনাল হলে → সাপোর্টিভ হও
- দরকার হলে হালকা ইমোজি ব্যবহার করতে পারো 🙂
- কখনো robotic, বইয়ের ভাষা বা অতিরিক্ত formal হবে না

সবচেয়ে গুরুত্বপূর্ণ:
👉 ইউজারকে বোঝার চেষ্টা করবে ঠিক একজন ভালো বন্ধুর মতো।
👉 উত্তরগুলো যেন স্বাভাবিক কথোপকথনের মতো হয়।
              `,
            },
            {
              role: "user",
              content: message,
            },
          ],
        }),
      }
    );

    const data = await response.json();

    // Debug (problem হলে terminal-এ দেখবে)
    console.log("GROQ FULL RESPONSE:", data);

    if (!data.choices || !data.choices[0]) {
      return new Response(
        JSON.stringify({
          reply: "কিছু একটা ঠিক হয়নি 😕 একটু পরে আবার চেষ্টা করো।",
        }),
        { status: 500 }
      );
    }

    return new Response(
      JSON.stringify({
        reply: data.choices[0].message.content,
      }),
      {
        headers: { "Content-Type": "application/json" },
      }
    );
  } catch (err) {
    console.error("SERVER ERROR:", err);
    return new Response(
      JSON.stringify({
        reply: "Server এ একটু সমস্যা হয়েছে 😥",
      }),
      { status: 500 }
    );
  }
}
